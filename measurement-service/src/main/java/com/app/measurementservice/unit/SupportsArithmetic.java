package com.app.measurementservice.unit;

public interface SupportsArithmetic extends IMeasurable {
    // Marker interface for units that support arithmetic operations
    // (add, subtract). Temperature does not support these.
}
